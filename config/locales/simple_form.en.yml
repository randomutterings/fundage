en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      # text: 'required'
      # mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      html: '<small style="color:#E33100;">Required</small>'
    # error_notification:
    #   default_message: "Please review the problems below:"
    placeholders:
      defaults:
        amount: '0.00'
    labels:
      debits:
        amount: 'Amount to withdraw'
        wallet: 'Transfer from'
      credits:
        amount: 'Amount to deposit'
        wallet: 'Transfer to'
    hints:
      defaults:
        name: 'required'
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    prompts:
      defaults:
        wallet: 'Select a wallet'
